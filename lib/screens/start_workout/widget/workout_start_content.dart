import 'package:flutter/material.dart';

class WorkoutStartContent extends StatelessWidget {
  const WorkoutStartContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
